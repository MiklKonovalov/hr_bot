# Установка API ключа OpenAI

Эта инструкция поможет вам получить и установить API ключ OpenAI для генерации сопроводительных писем.

## Шаг 1: Получение API ключа OpenAI

1. Перейдите на сайт [OpenAI Platform](https://platform.openai.com/)
2. Войдите в свой аккаунт или создайте новый
3. Перейдите в раздел [API Keys](https://platform.openai.com/api-keys)
4. Нажмите кнопку **"Create new secret key"**
5. Скопируйте созданный ключ (он будет показан только один раз!)

⚠️ **Важно**: Сохраните ключ в безопасном месте. Если вы его потеряете, придется создать новый.

## Шаг 2: Установка API ключа

### Вариант A: Использование файла .env (Рекомендуется)

1. Создайте файл `.env` в корне проекта (если его еще нет):

```bash
touch .env
```

2. Откройте файл `.env` и добавьте строку:

```bash
OPENAI_API_KEY=sk-ваш_ключ_здесь
```

**Пример:**
```bash
OPENAI_API_KEY=sk-proj-abc123def456ghi789jkl012mno345pqr678stu901vwx234yz
```

3. Сохраните файл

⚠️ **Важно**: 
- Не добавляйте пробелы вокруг знака `=`
- Не используйте кавычки вокруг значения (если только значение не содержит пробелы)
- Файл `.env` уже добавлен в `.gitignore`, чтобы не попасть в репозиторий

### Вариант B: Экспорт переменной окружения

#### Для macOS/Linux:

```bash
export OPENAI_API_KEY="sk-ваш_ключ_здесь"
```

Чтобы переменная сохранялась после перезапуска терминала, добавьте в файл `~/.zshrc` (или `~/.bashrc`):

```bash
echo 'export OPENAI_API_KEY="sk-ваш_ключ_здесь"' >> ~/.zshrc
source ~/.zshrc
```

#### Для Windows (PowerShell):

```powershell
$env:OPENAI_API_KEY="sk-ваш_ключ_здесь"
```

Для постоянной установки:

```powershell
[System.Environment]::SetEnvironmentVariable('OPENAI_API_KEY', 'sk-ваш_ключ_здесь', 'User')
```

## Шаг 3: Проверка установки

Запустите бота и попробуйте сгенерировать письмо. Если ключ установлен правильно, вы увидите:

```
✅ OpenAI клиент инициализирован
🔄 Отправляю запрос к OpenAI API...
✅ Письмо успешно сгенерировано через OpenAI
```

В Telegram вы увидите:
```
✨ Письмо сгенерировано с помощью OpenAI AI
```

Если ключ не установлен или неверный, вы увидите:
```
⚠️ OpenAI API ключ не установлен. Использую шаблонную генерацию.
```

Или в Telegram:
```
📋 Письмо сгенерировано по шаблону с анализом резюме
⚠️ Не удалось использовать OpenAI: API ключ OpenAI не установлен
```

## Возможные проблемы

### 1. "API ключ OpenAI не установлен"

**Решение:**
- Убедитесь, что файл `.env` находится в корне проекта (там же, где `telegram_vacancy_bot.py`)
- Проверьте, что в `.env` нет лишних пробелов: `OPENAI_API_KEY=sk-...` (без пробелов)
- Убедитесь, что установлен `python-dotenv`: `pip3 install python-dotenv`

### 2. "Неверный API ключ OpenAI"

**Решение:**
- Проверьте, что ключ скопирован полностью (начинается с `sk-`)
- Убедитесь, что ключ не истек (проверьте на [platform.openai.com](https://platform.openai.com/api-keys))
- Создайте новый ключ, если старый не работает

### 3. "OpenAI недоступен в вашем регионе"

**Решение:**
- OpenAI может быть недоступен в некоторых странах
- Используйте VPN или прокси
- Или используйте шаблонную генерацию (она работает без OpenAI)

### 4. "Превышен лимит запросов к OpenAI"

**Решение:**
- Проверьте баланс на [platform.openai.com/billing](https://platform.openai.com/billing)
- Подождите некоторое время перед повторной попыткой
- Рассмотрите возможность обновления тарифа

## Стоимость использования OpenAI API

- OpenAI API платный, но есть бесплатный кредит для новых пользователей
- Стоимость генерации одного письма: ~$0.001-0.002 (очень дешево)
- Проверьте текущие тарифы на [platform.openai.com/pricing](https://platform.openai.com/pricing)

## Безопасность

⚠️ **Важные правила безопасности:**

1. **Никогда не публикуйте API ключ** в открытых репозиториях
2. **Не делитесь ключом** с другими людьми
3. **Используйте файл `.env`** и убедитесь, что он в `.gitignore`
4. **Регулярно проверяйте** использование ключа на platform.openai.com
5. **Удаляйте неиспользуемые ключи**

## Альтернатива: Шаблонная генерация

Если вы не хотите использовать OpenAI API, бот автоматически переключится на шаблонную генерацию, которая:
- Работает без API ключа
- Анализирует требования из вакансии
- Сопоставляет их с вашим резюме
- Генерирует персонализированное письмо

Шаблонная генерация менее гибкая, но полностью бесплатна и работает офлайн.
