# Решение проблем с Telegram ботом

## Проблема: Кнопки не работают / "Ничего не происходит"

### Возможные причины:

1. **Бот не запущен или остановлен**
   - Убедитесь, что бот запущен: `python3 telegram_vacancy_bot.py`
   - Бот должен работать постоянно для обработки callback

2. **Обработчик callback не зарегистрирован**
   - Проверьте, что в коде есть: `application.add_handler(CallbackQueryHandler(bot.handle_callback))`
   - Обработчик должен быть зарегистрирован ДО `application.run_polling()`

3. **Проблема с event loop**
   - Убедитесь, что event loop правильно инициализирован
   - Для Python 3.9+ добавлена автоматическая инициализация

4. **Ошибки в консоли**
   - Проверьте вывод в терминале, где запущен бот
   - Должны быть сообщения вида: `🔔 Получен callback: yes_...`

### Диагностика:

1. **Проверьте логи бота:**
   ```bash
   python3 telegram_vacancy_bot.py
   ```
   При нажатии кнопки должны появиться сообщения:
   - `🔔 Получен callback: yes_...`
   - `📝 Начало генерации письма...`

2. **Проверьте, что бот получает обновления:**
   - Напишите боту `/start`
   - Бот должен ответить

3. **Проверьте регистрацию обработчиков:**
   - В коде должно быть: `CallbackQueryHandler(bot.handle_callback)`
   - Обработчик должен быть добавлен перед `run_polling()`

### Решение:

1. Перезапустите бота
2. Проверьте логи в консоли
3. Убедитесь, что бот запущен и работает
4. Попробуйте отправить вакансии заново командой `/send`

## Проблема: OpenAI API недоступен (403)

### Решение:
- Бот автоматически переключается на шаблонную генерацию
- Шаблонная генерация использует резюме для персонализации
- Письма будут генерироваться без OpenAI API

## Проблема: Резюме не загружается

### Решение:
1. Проверьте формат файла (PDF, DOC, DOCX, TXT)
2. Установите библиотеки: `pip3 install PyPDF2 python-docx`
3. Попробуйте отправить резюме текстовым сообщением
