# Пошаговая инструкция: размещение Telegram-бота на Railway

Инструкция поможет развернуть бота вакансий на [Railway](https://railway.app) так, чтобы он работал 24/7 независимо от вашего компьютера.

---

## Что понадобится заранее

- [ ] Аккаунт на [GitHub](https://github.com) (или GitLab) с репозиторием проекта `hr_app`
- [ ] Аккаунт на [Railway](https://railway.app) (можно войти через GitHub)
- [ ] Токен бота от [@BotFather](https://t.me/BotFather) (`TELEGRAM_BOT_TOKEN`)
- [ ] Chat ID канала или чата (`TELEGRAM_CHAT_ID`) — как в вашем `.env`
- [ ] Опционально: `OPENAI_API_KEY`, `HH_ACCESS_TOKEN` — если используете генерацию писем и отклики через HH

---

## Шаг 1. Подготовить репозиторий

Бот будет запускаться из кода в Git. Убедитесь, что проект запушен в GitHub (или другой Git-хостинг).

1. **Инициализировать Git (если ещё не сделано):**
   ```bash
   cd /Users/konovalovmihail/hr_app
   git init
   git add .
   git commit -m "Prepare for Railway deployment"
   ```

2. **Создать репозиторий на GitHub** (например, `hr_app` или `telegram-vacancy-bot`), затем:
   ```bash
   git remote add origin https://github.com/ВАШ_ЛОГИН/hr_app.git
   git branch -M main
   git push -u origin main
   ```

3. **Не добавляйте в репозиторий секреты.** Файл `.env` должен быть в `.gitignore`:
   ```bash
   echo ".env" >> .gitignore
   git add .gitignore && git commit -m "Ignore .env" && git push
   ```
   Переменные (токен, chat id) вы зададите в Railway отдельно.

---

## Шаг 2. Создать проект в Railway

1. Откройте [railway.app](https://railway.app) и войдите (через GitHub удобнее).

2. Нажмите **«New Project»** (или **«Start a New Project»**).

3. Выберите **«Deploy from GitHub repo»**.

4. Разрешите Railway доступ к GitHub, если попросит. Выберите репозиторий с ботом (например, `hr_app`).

5. Укажите репозиторий и ветку (обычно `main`). Railway создаст проект и начнёт первый деплой. Его можно будет донастроить в следующих шагах.

---

## Шаг 3. Настроить сервис (корневая директория и команда запуска)

1. В проекте Railway откройте созданный **сервис** (один блок/карточка).

2. Перейдите в **«Settings»** (Настройки).

3. **Root Directory (если код не в корне репо):**  
   Если бот лежит в подпапке (например, `hr_app/` внутри репозитория), в поле **Root Directory** укажите эту папку, например: `hr_app`.  
   Если репозиторий содержит только код бота в корне — оставьте поле пустым.

4. **Build Command:**  
   Оставьте пустым или укажите:
   ```bash
   pip install -r requirements.txt
   ```
   Railway по умолчанию ставит зависимости из `requirements.txt` для Python-проектов.

5. **Start Command (команда запуска):**  
   Укажите явно:
   ```bash
   python3 telegram_vacancy_bot.py
   ```
   Если в репозитории есть `Procfile` с процессом `worker`, Railway может подхватить команду из него — тогда в Start Command может быть пусто или то же самое. Лучше задать команду здесь, чтобы не зависеть от типа процесса.

6. Сохраните настройки (если есть кнопка **Save**).

---

## Шаг 4. Добавить переменные окружения

Бот читает настройки из переменных окружения (как из `.env` локально). Их нужно задать в Railway.

1. В карточке сервиса откройте вкладку **«Variables»** (Переменные).

2. Нажмите **«+ New Variable»** или **«Add Variable»** и добавьте по одной переменной:

   | Имя переменной       | Значение                    | Обязательно |
   |----------------------|-----------------------------|-------------|
   | `TELEGRAM_BOT_TOKEN` | Токен от @BotFather         | Да          |
   | `TELEGRAM_CHAT_ID`   | ID чата/канала (число или @channel) | Да   |
   | `OPENAI_API_KEY`     | Ключ OpenAI (для генерации писем) | Нет  |
   | `HH_ACCESS_TOKEN`    | Токен HH (для откликов)     | Нет         |

   Значения вставляйте без кавычек и без пробелов в начале/конце.

3. После добавления переменных Railway может автоматически перезапустить деплой. Дождитесь окончания сборки и запуска.

---

## Шаг 5. Проверить сборку и логи

1. Откройте вкладку **«Deployments»**. Должен быть последний деплой в статусе **Success** (или **Active**).

2. Откройте вкладку **«Logs»** (или нажмите на деплой и откройте логи). Должны появиться строки вроде:
   - `✅ Переменные загружены из .env файла` (или из переменных Railway)
   - `🤖 Бот запущен! Ожидаю команды...`

3. Если в логах есть ошибка (например, `TELEGRAM_BOT_TOKEN` не задан), вернитесь в **Variables** и проверьте имена и значения переменных.

---

## Шаг 6. Проверить бота в Telegram

1. Откройте бота в Telegram и отправьте команду **/start**.

2. Бот должен ответить приветствием и меню. Если ответа нет — снова проверьте логи в Railway и переменные `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`.

3. Проверьте команды **/help**, **/send** (или пункты меню) по необходимости.

---

## Шаг 7. Тариф и круглосуточная работа

- **Проблема:** на бесплатном тарифе Railway сервис может «засыпать» после периода неактивности, и бот перестанет отвечать.
- **Решение для 24/7:** перейти на платный тариф (например, **Developer** или **Team**), чтобы сервис не отключался. Тогда бот будет соответствовать критериям круглосуточной доступности.

В **Settings** сервиса можно посмотреть текущий план и при необходимости обновить тариф.

---

## Дальнейшие действия

### Обновление кода (новый деплой)

1. Внесите изменения в код локально.
2. Закоммитьте и запушьте в ту же ветку (например, `main`):
   ```bash
   git add .
   git commit -m "Update bot"
   git push origin main
   ```
3. Railway автоматически соберёт и задеплоит новую версию. Простой обычно не более 30 секунд.

### Данные между деплоями

- Файлы `sent_vacancies.json`, `users_data.json`, `resumes/` и т.п. хранятся внутри контейнера. При новом деплое контейнер пересоздаётся, и эти данные сбрасываются.
- Если нужна сохранность данных между деплоями, можно позже подключить **Railway Volume** (том) и изменить в коде пути к файлам на директорию тома — это выходит за рамки данной инструкции.

---

## Краткий чек-лист

- [ ] Репозиторий на GitHub, `.env` в `.gitignore`
- [ ] Новый проект Railway, деплой из этого репозитория
- [ ] В настройках сервиса: **Start Command** = `python3 telegram_vacancy_bot.py`
- [ ] В **Variables** заданы `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`
- [ ] Деплой успешен, в логах есть сообщение о запуске бота
- [ ] В Telegram бот отвечает на /start
- [ ] Для 24/7 включён платный тариф (без «сна»)

---

## Возможные ошибки и решения

| Проблема | Что проверить |
|----------|----------------|
| Деплой падает с ошибкой | Логи сборки (Build logs): версия Python, установка `requirements.txt`. В корне можно добавить `runtime.txt` с содержимым `python-3.11.0` (или нужной версией). |
| «TELEGRAM_BOT_TOKEN not set» | Вкладка Variables: имя переменной точно `TELEGRAM_BOT_TOKEN`, значение без пробелов. |
| Бот не отвечает в Telegram | Логи сервиса (Runtime logs); правильность `TELEGRAM_CHAT_ID`; бот не заблокирован пользователем. |
| Бот «засыпает» через некоторое время | Переход на платный тариф Railway, чтобы сервис не выключался. |

Если нужно явно зафиксировать версию Python на Railway, создайте в корне проекта (или в Root Directory) файл `runtime.txt` с одной строкой, например:
```
python-3.11.9
```

После выполнения всех шагов бот будет работать на Railway и оставаться доступным при выключенном локальном компьютере.
