# Фильтры вакансий

Система поддерживает фильтрацию вакансий по зарплате и опыту работы. Вакансии показываются, если они удовлетворяют **ВСЕМ заданным** фильтрам (логическое И).

## Ограничение количества вакансий

По умолчанию система выводит **10 вакансий**. Это ограничение можно настроить через переменную окружения `MAX_VACANCIES`.

## Настройка фильтров

### Вариант 1: Через переменные окружения (рекомендуется)

Добавьте в файл `.env`:

```bash
MIN_SALARY=250000
MIN_EXPERIENCE_YEARS=3
MAX_VACANCIES=20
```

Где:
- `MIN_SALARY` - минимальная зарплата (руб.)
- `MIN_EXPERIENCE_YEARS` - минимальный опыт (лет)
- `MAX_VACANCIES` - максимальное количество вакансий для вывода (по умолчанию 10)

### Вариант 2: Через параметры при запуске

Можно передать фильтры при создании объекта:

```python
from ios_vacancies_finder import ProductManagerVacancyFinder

finder = ProductManagerVacancyFinder(
    min_salary=250000,
    min_experience_years=3
)
vacancies = finder.find_all_vacancies()
```

## Как работают фильтры

### Фильтр по зарплате

- Проверяет минимальную зарплату из диапазона (если указан диапазон)
- Если указана только максимальная зарплата, проверяет её
- Парсит зарплату из строки для вакансий из разных источников
- Вакансия проходит фильтр, если её зарплата >= `MIN_SALARY`

**Примеры:**
- `"250,000 - 350,000 RUR"` → проверяется 250,000
- `"от 300,000 RUR"` → проверяется 300,000
- `"до 400,000 RUR"` → проверяется 400,000

### Фильтр по опыту

- Использует данные из HeadHunter API (если доступны)
- Парсит опыт из названия опыта (например, "от 3 лет")
- Парсит опыт из описания вакансии
- Вакансия проходит фильтр, если требуемый опыт >= `MIN_EXPERIENCE_YEARS`

**Маппинг опыта из HH API:**
- `noExperience` = 0 лет
- `between1And3` = 1-3 года (минимум 1)
- `between3And6` = 3-6 лет (минимум 3)
- `moreThan6` = более 6 лет (минимум 6)

## Логика фильтрации

Вакансия **показывается**, если она удовлетворяет:
- **ВСЕМ заданным фильтрам** (логическое И)

То есть:
- Если задан только фильтр по зарплате → вакансия должна иметь зарплату >= указанного значения
- Если задан только фильтр по опыту → вакансия должна требовать опыт >= указанного значения
- Если заданы оба фильтра → вакансия должна удовлетворять **ОБОИМ** условиям одновременно

**Пример:** Если заданы фильтры `MIN_SALARY=250000` и `MIN_EXPERIENCE_YEARS=3`, то вакансия будет показана только если:
- Зарплата >= 250,000 руб. **И**
- Требуемый опыт >= 3 лет

## Примеры использования

### Пример 1: Только фильтр по зарплате

```bash
# .env
MIN_SALARY=300000
```

Показываются вакансии с зарплатой от 300,000 руб., независимо от опыта.

### Пример 2: Только фильтр по опыту

```bash
# .env
MIN_EXPERIENCE_YEARS=5
```

Показываются вакансии, требующие опыт от 5 лет, независимо от зарплаты.

### Пример 3: Оба фильтра

```bash
# .env
MIN_SALARY=250000
MIN_EXPERIENCE_YEARS=3
```

Показываются вакансии, которые:
- Имеют зарплату >= 250,000 руб. **ИЛИ**
- Требуют опыт >= 3 лет

## Отключение фильтров

Чтобы отключить фильтры, просто не указывайте переменные окружения или установите их в пустые значения:

```bash
# .env
# MIN_SALARY=
# MIN_EXPERIENCE_YEARS=
```

Или удалите эти строки из `.env`.

## Вывод информации

При запуске скрипт показывает, какие фильтры активны:

```
💰 Фильтр по зарплате: от 250,000 руб.
📅 Фильтр по опыту: от 3 лет
ℹ️  Вакансии должны удовлетворять хотя бы одному из фильтров

🚀 Начинаю поиск вакансий Product Manager...
```

После фильтрации показывается статистика:

```
📊 После фильтрации: 7 из 15 вакансий
```

## Примечания

1. Если зарплата не указана в вакансии, она не пройдет фильтр по зарплате
2. Если опыт не указан в вакансии, она не пройдет фильтр по опыту
3. Вакансия может пройти по одному фильтру, даже если не проходит по другому
4. Фильтры применяются после удаления дубликатов, но до ограничения до 10 вакансий
